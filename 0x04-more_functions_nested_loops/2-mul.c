#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second nmber to multiply with first number
 * Return: The result of multiplication between x and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
